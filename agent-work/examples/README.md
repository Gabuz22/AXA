# Exemples de démonstration

Fixtures produites à l'installation (origin `example_fixture`/`mock`). Elles illustrent le FORMAT de chaque agent et **ne sont pas de vraies propositions**. Le coordinateur les exclut de la priorisation. Ne pas appliquer.
